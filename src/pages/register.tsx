import { RegisterForm } from "@/components/auth/register-form";

export default function Register() {
  return (
    <div className="max-h-screen w-full">
      <RegisterForm className="mx-auto mt-24 max-w-lg"></RegisterForm>
    </div>
  );
}
